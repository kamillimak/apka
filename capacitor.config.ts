import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capcutpro.web',
  appName: 'CapCut PRO Web',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
